%div{:class => "#{tile_to_column(tile.format)} columns #{tile.format} tile"}
  = content_tag_for(:div, tile.course, :style => "background: url('#{tile_image(tile)}') no-repeat top left") do
    %div.information
      %div.textblock
        %h1
          = tile.title
        %h2
          = tile.subtitle
