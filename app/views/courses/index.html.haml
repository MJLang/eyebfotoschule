%section#overview
  .row
    .tilegrid
      .small-12.columns
        - @courses.each_with_index do |course, index|
          = render course


#firstAnchor

  -# = render :partial => 'information', :locals => {:course => Course.find(20)}