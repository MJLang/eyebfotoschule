if ($("#firstAnchor").length > 0) {
  $("#firstAnchor").replaceWith("<%= j render :partial => 'information', :locals => {:course => @course, :booking => @booking, :training => @training} %>");
} else {
  $("#coursesection").replaceWith("<%= j render :partial => 'information', :locals => {:course => @course, :booking => @booking, :training => @training} %>");
}

$('.chzn-select').chosen(
  {
    disable_search_threshold: 20
  });
$("#booking_training_attributes_date").datepicker({
    dateFormat: "dd/mm/yy",
    altFormat: "dd-mm-yyyy"
  });
$("#coursesection").ScrollTo({
  duration: 2000,
  easing: 'linear'
});

$("#backToTop").click(function() {
  $("#overview").ScrollTo();
});
  