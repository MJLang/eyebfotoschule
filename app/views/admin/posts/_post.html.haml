= content_tag_for(:li, post) do 
  %p{:class => "#{post.published ? 'published' : 'pending'}"}
    = post.title
    -
    = link_to "Bearbeiten", edit_admin_post_path(post)
    - 
    = link_to "X", admin_post_path(post), method: :delete